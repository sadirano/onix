name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            goos: windows
            goarch: amd64
            suffix: .exe
            pkg_cmd: powershell
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
            suffix: ""
            pkg_cmd: bash
          - os: ubuntu-latest
            goos: linux
            goarch: arm64
            suffix: ""
            pkg_cmd: bash
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4.3.1

      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff  # v5.6.0
        with:
          go-version-file: go.mod

      - name: Build
        run: >
          go build
          -trimpath
          -ldflags "-s -w -X main.buildVersion=${{ github.ref_name }}"
          -o onix${{ matrix.suffix }}
          .
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}

      - name: Package (Windows)
        if: matrix.goos == 'windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path dist | Out-Null
          Copy-Item onix.exe dist\
          $zip = "onix-${{ github.ref_name }}-${{ matrix.goos }}-${{ matrix.goarch }}.zip"
          Compress-Archive -Path dist\* -DestinationPath $zip
          $hash = (Get-FileHash $zip -Algorithm SHA256).Hash.ToLower()
          Write-Host "SHA256: $hash"

      - name: Package (Linux)
        if: matrix.goos == 'linux'
        run: |
          mkdir -p dist
          cp onix dist/
          tar -czf "onix-${{ github.ref_name }}-${{ matrix.goos }}-${{ matrix.goarch }}.tar.gz" -C dist .
          sha256sum "onix-${{ github.ref_name }}-${{ matrix.goos }}-${{ matrix.goarch }}.tar.gz" || true

      - name: Release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65  # v2.6.2
        with:
          files: |
            onix-${{ github.ref_name }}-${{ matrix.goos }}-${{ matrix.goarch }}.zip
            onix-${{ github.ref_name }}-${{ matrix.goos }}-${{ matrix.goarch }}.tar.gz
          generate_release_notes: true
