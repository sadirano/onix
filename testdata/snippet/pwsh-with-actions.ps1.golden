# onix shell integration (generated; do not edit — run 'onix sync')
# Source from $PROFILE: . '/ONIX_HOME/shell/onix.ps1'

$global:onixExe = '/ONIX_EXE'

$onixAliasCompleter = {
    param($wordToComplete, $commandAst, $cursorPosition)
    @(& $global:onixExe list-names 2>$null) |
        Where-Object { $_ -like "$wordToComplete*" } |
        ForEach-Object {
            [System.Management.Automation.CompletionResult]::new($_, $_, 'ParameterValue', $_)
        }
}

function global:o {
    [CmdletBinding()]
    param(
        [Parameter(Position=0, Mandatory=$false)][string]$Alias,
        [Parameter(Position=1, Mandatory=$false)][string]$Path
    )
    if (-not $Alias) {
        & $global:onixExe aliases
        return
    }
    if ($Path) {
        # 'o foo C:\some\path' — register (or update) the alias and cd
        # into it. The directory is auto-created by 'onix add' if it
        # doesn't exist.
        $resolved = & $global:onixExe add $Alias $Path
    } else {
        $resolved = & $global:onixExe resolve $Alias
    }
    if ($LASTEXITCODE -eq 0) {
        Set-Location -LiteralPath $resolved
        & $global:onixExe context apply $Alias | Invoke-Expression
    }
}
function global:n {
    [CmdletBinding()]
    param([Parameter(Position=0, Mandatory=$true)][string]$Alias)
    & $global:onixExe edit $Alias
}
function global:s {
    [CmdletBinding()]
    param([Parameter(Position=0, Mandatory=$true)][string]$Alias)
    & $global:onixExe explore $Alias
}
function global:y {
    [CmdletBinding()]
    param([Parameter(Position=0, Mandatory=$true)][string]$Alias)
    & $global:onixExe yank $Alias
}
function global:r {
    [CmdletBinding()]
    param(
        [Parameter(Position=0, Mandatory=$true)][string]$Alias,
        [Parameter(Position=1, Mandatory=$true, ValueFromRemainingArguments=$true)][string[]]$Rest
    )
    & $global:onixExe run $Alias -- @Rest
}
function global:sg {
    [CmdletBinding()]
    param(
        [Parameter(Position=0, Mandatory=$true)][string]$Alias,
        [Parameter(Position=1, ValueFromRemainingArguments=$true)][string[]]$Rest
    )
    & $global:onixExe grep $Alias @Rest
}
function global:ff {
    [CmdletBinding()]
    param(
        [Parameter(Position=0, Mandatory=$true)][string]$Alias,
        [Parameter(Position=1, ValueFromRemainingArguments=$true)][string[]]$Rest
    )
    & $global:onixExe find $Alias @Rest
}

function global:test {
    [CmdletBinding()]
    param(
        [Parameter(Position=0, Mandatory=$true)][string]$Alias,
        [Parameter(Position=1, ValueFromRemainingArguments=$true)][string[]]$Rest
    )
    & $global:onixExe exec test $Alias -- @Rest
}

function global:pr {
    [CmdletBinding()]
    param(
        [Parameter(Position=0, Mandatory=$true)][string]$Alias,
        [Parameter(Position=1, ValueFromRemainingArguments=$true)][string[]]$Rest
    )
    & $global:onixExe exec pr $Alias -- @Rest
}

Register-ArgumentCompleter -CommandName ff,n,o,pr,r,s,sg,test,y -ParameterName Alias -ScriptBlock $onixAliasCompleter
